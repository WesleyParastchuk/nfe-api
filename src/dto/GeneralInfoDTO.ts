export interface GeneralInfoDTO {
  emissionType: string;
  invoiceNumber: number;
  series: number;
  emissionDate: number;
  authorizationProtocol: string;
  protocolDate: number;
  environment: string;
  xmlVersion: string;
  xsltVersion: string;
}
